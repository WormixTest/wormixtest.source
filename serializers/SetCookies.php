<?

class SetCookies
{
	static $command_id = 136;

	static function handle($callback, $user_id, $input)
	{
		# $names = $input->read_string_array();
		# $values = $input->read_string_array();

		# ответа не должно быть, вообще
		# $callback->socket_write(10136, $output);
	}

}